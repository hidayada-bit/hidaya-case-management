warning: in the working copy of 'src/App.jsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/App.jsx b/src/App.jsx[m
[1mindex 0536ef5..e42cbbe 100644[m
[1m--- a/src/App.jsx[m
[1m+++ b/src/App.jsx[m
[36m@@ -2181,6 +2181,72 @@[m [mfunction LoginPage({ onLogin, onSwitchToSignup }) {[m
   )[m
 }[m
 [m
[32m+[m[32m// ─── SETTINGS PAGE ────────────────────────────────────────────────────────────[m
[32m+[m[32mfunction SettingsPage({ user, onUpdateUser, toast, isMobile }) {[m
[32m+[m[32m  const [fullName, setFullName] = useState(user?.full_name || '')[m
[32m+[m[32m  const [photoFile, setPhotoFile] = useState(null)[m
[32m+[m[32m  const [photoPreview, setPhotoPreview] = useState(user?.profile_photo_url || null)[m
[32m+[m[32m  const [newPass, setNewPass] = useState('')[m
[32m+[m[32m  const [saving, setSaving] = useState(false)[m
[32m+[m
[32m+[m[32m  const handleSave = async () => {[m
[32m+[m[32m    if (!fullName) { toast('Full name is required', 'error'); return }[m
[32m+[m[32m    setSaving(true)[m
[32m+[m[32m    try {[m
[32m+[m[32m      let profile_photo_url = user?.profile_photo_url || null[m
[32m+[m[32m      if (photoFile) {[m
[32m+[m[32m        const path = `${user.id}/profile.jpg`[m
[32m+[m[32m        profile_photo_url = await uploadFile({ supabase, file: photoFile, bucket: BUCKETS.STAFF_PHOTOS, path, type: 'photo' })[m
[32m+[m[32m      }[m
[32m+[m[32m      const { error } = await supabase.from('users').update({ full_name: fullName, profile_photo_url }).eq('id', user.id)[m
[32m+[m[32m      if (error) throw error[m
[32m+[m[32m      if (newPass) {[m
[32m+[m[32m        const { error: passErr } = await supabase.auth.updateUser({ password: newPass })[m
[32m+[m[32m        if (passErr) throw passErr[m
[32m+[m[32m        setNewPass('')[m
[32m+[m[32m      }[m
[32m+[m[32m      onUpdateUser({ ...user, full_name: fullName, profile_photo_url })[m
[32m+[m[32m      toast('Settings saved', 'success')[m
[32m+[m[32m    } catch (e) { toast('Failed to save: ' + e.message, 'error') }[m
[32m+[m[32m    finally { setSaving(false) }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  return ([m
[32m+[m[32m    <div style={{ maxWidth: 480, margin: '0 auto', padding: isMobile ? '16px 4px' : '24px 0' }}>[m
[32m+[m[32m      <div style={{ background: '#fff', borderRadius: 16, border: `1px solid ${B.border}`, padding: isMobile ? 20 : 28, boxShadow: `0 2px 12px rgba(30,125,34,0.06)` }}>[m
[32m+[m[32m        <div style={{ fontWeight: 800, fontSize: 16, color: B.text, marginBottom: 20 }}>My Profile</div>[m
[32m+[m
[32m+[m[32m        <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 20 }}>[m
[32m+[m[32m          <ImageUpload label="Profile Photo" type="photo" currentUrl={photoPreview}[m
[32m+[m[32m            onFileReady={file => { setPhotoFile(file); setPhotoPreview(URL.createObjectURL(file)) }}[m
[32m+[m[32m            shape="circle" previewSize={88} />[m
[32m+[m[32m        </div>[m
[32m+[m
[32m+[m[32m        <FI label="Full Name" value={fullName} onChange={setFullName} placeholder="Your full name" req />[m
[32m+[m
[32m+[m[32m        <div style={{ marginBottom: 16 }}>[m
[32m+[m[32m          <FL>Email Address</FL>[m
[32m+[m[32m          <input value={user?.email || ''} disabled style={{ ...inputBase, background: B.bg, color: B.textLight, cursor: 'not-allowed' }} />[m
[32m+[m[32m        </div>[m
[32m+[m
[32m+[m[32m        <div style={{ marginBottom: 4 }}>[m
[32m+[m[32m          <FL>Role</FL>[m
[32m+[m[32m          <input value={(user?.role || 'staff').replace(/^\w/, c => c.toUpperCase())} disabled style={{ ...inputBase, background: B.bg, color: B.textLight, cursor: 'not-allowed' }} />[m
[32m+[m[32m        </div>[m
[32m+[m
[32m+[m[32m        <div style={{ height: 1, background: B.border, margin: '20px 0' }} />[m
[32m+[m
[32m+[m[32m        <div style={{ fontWeight: 700, fontSize: 13, color: B.text, marginBottom: 12 }}>Change Password</div>[m
[32m+[m[32m        <FI label="New Password" value={newPass} onChange={setNewPass} type="password" placeholder="Leave blank to keep current password" />[m
[32m+[m
[32m+[m[32m        <Btn onClick={handleSave} variant="primary" full disabled={saving}>[m
[32m+[m[32m          {saving ? 'Saving…' : 'Save Changes'}[m
[32m+[m[32m        </Btn>[m
[32m+[m[32m      </div>[m
[32m+[m[32m    </div>[m
[32m+[m[32m  )[m
[32m+[m[32m}[m
[32m+[m
 // ─── ROOT APP ─────────────────────────────────────────────────────────────────[m
 export default function App() {[m
   const bp = useBreakpoint()[m
[36m@@ -2485,11 +2551,7 @@[m [mexport default function App() {[m
           {page === 'academics' && <AcademicsPage children={children} isMobile={isMobile} />}[m
           {page === 'messaging' && <MessagingPage families={families} isMobile={isMobile} />}[m
           {page === 'settings' && ([m
[31m-            <div style={{ padding: '60px 20px', textAlign: 'center', color: B.textLight }}>[m
[31m-              <div style={{ fontSize: 48, marginBottom: 12 }}>⚙️</div>[m
[31m-              <div style={{ fontWeight: 700, fontSize: 16, color: B.text, marginBottom: 6 }}>Settings</div>[m
[31m-              <div style={{ fontSize: 13 }}>User management coming soon.</div>[m
[31m-            </div>[m
[32m+[m[32m            <SettingsPage user={user} onUpdateUser={setUser} toast={toast} isMobile={isMobile} />[m
           )}[m
         </div>[m
       </main>[m
